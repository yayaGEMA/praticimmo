{# si il existe ne memoire flash des messages de type error on les affiche tous avec une boucle #}
{% for message in app.flashes('error') %}
<div class="row">
    <div class="alert alert-danger text-center col-12 my-2">{{ message }}</div>
</div>
{% endfor %}