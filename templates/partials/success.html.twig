{# si il existe ne memoire flash des messages de type succes on les affiche tous avec une boucle #}
{% for message in app.flashes('success') %}
<div class="row">
    <div class="alert alert-success text-center col-12 my-2">{{ message }}</div>
</div>
{% endfor %}